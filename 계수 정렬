#계수 정렬
#리스트 안의 데이터가 모두 정수일 때만 가능하다.
#데이터를 하나씩 확인하며 데이터의 값과
#동일한 인덱스의 데이터를 1씩 증가시키는 방법의 정렬
import sys
array = list(map(int, sys.stdin.readline().split()))

count = [0] * (max(array) + 1)

for i in range(len(array)):
    count[array[i]] += 1

for i in range(len(array)):
    for j in range(count[i]):
        print(i, end = ' ')
