#선택정렬
#리스트의 인덱스 0의 값부터 시작해서 리스트 내의 모든 값을
#비교하면서 리스트 내의 제일 작은 값과 swap하면서 오름차순으로 정렬한다.
import sys

array = list(map(int, sys.stdin.readline().split()))

for i in range(len(array)):
    #비교 인덱스를 min_index에 저장한다.
    min_index = i
    for j in range(i + 1, len(array)):
        #리스트 내에 비교 인덱스에 위치한 값과 비교했을 때
        #제일 작은 값을 찾는다.
        if array[min_index] > array[j]:
            #리스트 안에 있는 가장 작은 값의 인덱스를
            #min_index에 저장한다.
            min_index = j
    #저장해둔 인덱스를 기반으로 비교 인덱스와 제일 작은 값이 있는 인덱스의
    #위치를 swap해준다.
    array[i], array[min_index] = array[min_index], array[i]


print(array)
